# Align every function to 64 bytes. Without it, adding unrelated code shifts
# the hot functions across cache-line and branch-predictor boundaries and
# moves small-array timings by 5-8% (measured with benchmarks/perf_check.py).
[build]
rustflags = ["-C", "llvm-args=-align-all-functions=6"]
